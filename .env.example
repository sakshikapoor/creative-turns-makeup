# Copy to .env for local development: cp .env.example .env
# Never commit .env.

# From the GitHub OAuth app (SETUP.md step 5)
GITHUB_CLIENT_ID=your-oauth-app-client-id
GITHUB_CLIENT_SECRET=your-oauth-app-client-secret
